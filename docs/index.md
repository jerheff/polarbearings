---
description: Polarbearings implements ~40 machine learning metrics as native Polars expressions — 2–4× faster than scikit-learn across ranking, classification, regression, and calibration.
---
<!-- Home page: reuse the root README verbatim so it stays the single source of
     truth. Edit README.md, not this file. The front matter above sets this
     page's SEO meta description; it lives here (not in README.md) so the
     GitHub README stays free of a rendered YAML table. -->
--8<-- "README.md"
