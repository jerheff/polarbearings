# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
.venv-wheel/
venv/
ENV/
env/

# Testing
.pytest_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
*.cover
.tox/

# IDEs / editor tooling
.vscode/
.idea/
# rope refactoring-library project state
.ropeproject/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Ruff
.ruff_cache/

# uv (project-local cache, if UV_CACHE_DIR is ever pointed here)
.uv-cache/

# Mutation testing
mutants/

# pytest-benchmark saved runs (generated by `just bench-compare`)
.benchmarks/

# MkDocs build output (`just docs-build` / `mkdocs build`); Read the Docs builds
# its own copy from source.
site/

# ---- Claude Code ----
# Shared & version-controlled: .claude/settings.json, CLAUDE.md,
# .claude/agents/, .claude/commands/, .claude/skills/, .claude/hooks/,
# .claude/agent-memory/, .mcp.json

# Per-machine settings & credentials
**/.claude/*.local.json
.mcp.local.json

# Local agent runtime state (machine-specific, regenerated)
**/.claude/worktrees/
**/.claude/projects/
**/.claude/shell-snapshots/
**/.claude/todos/
**/.claude/statsig/
**/.claude/.cc-writes/
**/.claude/memory/
**/.claude/agent-memory-local/
